'''bmi_calc를 만들기 위한 사전 준비'''# age = input("당신의 나이는 몇 살입니까? >>> ")# print(type(age))# print(f"당신은 내년에 {age+1}살이 됩니다.")'''input() 함수으 결과값은 언제나 str입니다. 즉 수학 연산을 위해서 과정 필요필요한 함수가 형변환 함수 (conversion) '''# age1 = input("당신의 나이는 몇 살입니까? >>>")# print(type(age1))       # 결과값 : str# age1_int = int(age1)    # str인 age1을 int로 자료형 변환# print(type(age1_int))# print(f"당신은 내년에 {age1_int+1}살이 됩니다")'''자주 쓰이는 형변환 함수1. int() -> str 또는 float을 int로 변경2. float() -> str 또는 int를 float로 변경'''# temp = int(3.8)# print(temp)# temp2 = float(4)# print(temp2)## temp3 = round(3.8)# print(temp3)# temp4 = round(5.465465, 2)      # 괄호 첫번째 수를 소수점 2째자리까지 표기# print(temp4)'''BMI 계산기를 작성1. 키(cm)를 입력 받아 변수 height에 저장2. 몸무게(kg)을 입력 weghit에 저장3. 몸무게/(키(m)의 제곱)을 계산하면 bmi 지수가 나옴4. bmi 지수를 int로 출력5. bmi를 소수점 셋째자리에서 반올림하여 둘째자리까지 출력 실행 예로고 출력 구글 text to ascii art 검색당신의 키는 몇 cm입니까? >>> 173.2당신의 몸무게는 몇  kg입니까? >>> 70당신의 bmi 지수는 23입니다.'''# height = input("당신의 키는 몇 cm 입니까? >>>")# height = float(height)# height = height / 100# weghit = input("당신의 몸무게는 몇 kg 입니까? >>>")# weghit = float(weghit)# bmi = weghit / (height**2)# bmi = round(bmi ,2)# bmi = input(f"당신의 bmi지수는 {bmi}입니다.")logo = """ ____  __  __  ____ (  _ \(  \/  )(_  _) ) _ < )    (  _)(_ (____/(_/\/\_)(____)"""print(logo)height = float(input("당신의 키는 몇 cm입니까? >>>")) / 100weghit = float(input("당신의 몸무게는 몇 kg입니까? >>>"))print(f"당신의 bmi지수는 {round(weghit/(height**2),2)}입니다.")